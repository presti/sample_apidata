name: sample_apidata
description: A sample project consuming an API

publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^8.1.2

  equatable: ^2.0.5

  get: ^4.6.5

  http: ^0.13.5

  url_launcher: ^6.1.10

  meta: ^1.8.0 # Referenced package

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
